<ul>
  <% if @recipe_type.errors.any? %>
    <li><%= flash[:errors] %></li>
    <% @recipe_type.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  <% end %>
</ul>
<%= form_for @recipe_type do |f| %>
  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>
  <%= f.submit 'Enviar' %>
<% end %>